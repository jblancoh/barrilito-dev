// Re-exports the same generated image for X/Twitter cards, so both platforms
// show an identical preview without duplicating the `ImageResponse` markup.
export { alt, contentType, default, size } from "./opengraph-image"
